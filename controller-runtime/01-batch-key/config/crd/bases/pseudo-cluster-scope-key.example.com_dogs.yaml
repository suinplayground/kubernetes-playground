---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: dogs.pseudo-cluster-scope-key.example.com
spec:
  group: pseudo-cluster-scope-key.example.com
  names:
    kind: Dog
    listKind: DogList
    plural: dogs
    singular: dog
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.foo
      name: Foo
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dog is the Schema for the dogs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DogSpec defines the desired state of Dog.
            properties:
              foo:
                description: Foo is an example field of Dog. Edit dog_types.go to
                  remove/update
                type: string
            type: object
          status:
            description: DogStatus defines the observed state of Dog.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
