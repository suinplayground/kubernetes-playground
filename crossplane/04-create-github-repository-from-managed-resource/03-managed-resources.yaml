apiVersion: repo.github.upbound.io/v1alpha1
kind: Repository
metadata:
  name: demo04-repo
  annotations:
    crossplane.io/external-name: demo04-repo
spec:
  forProvider:
    name: demo04-repo
    visibility: private
    description: "Created by Crossplane using the GitHub Provider with personal access token. Source repo: https://github.com/suinplayground/kubernetes-playground"
